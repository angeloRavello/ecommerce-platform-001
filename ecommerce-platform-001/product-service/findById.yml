info:
  name: findById
  type: http
  seq: 1

http:
  method: GET
  url: "{{product_service_url}}/api/products/:productId"
  params:
    - name: productId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
