info:
  name: reserve
  type: http
  seq: 3

http:
  method: POST
  url: "{{product_service_url}}/api/products/reserve"
  body:
    type: json
    data: |-
      {
        "reservations": [
          {
            "productId": 4,
            "quantity": 3
          },
          {
            "productId": 5,
            "quantity": 3
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
