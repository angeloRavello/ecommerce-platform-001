info:
  name: findAll
  type: http
  seq: 2

http:
  method: GET
  url: "{{product_service_url}}/api/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
