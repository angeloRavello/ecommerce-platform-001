info:
  name: findAll
  type: http
  seq: 2

http:
  method: GET
  url: "{{order_service_url}}/api/orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
